<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <title>Meine Aufgaben</title>
</head>
<body>
    <h1>Aufgabenliste</h1>

    <form action="/add" method="POST">
        <input type="text" name="title" placeholder="Aufgabe eingeben" required>
        <button type="submit">Hinzufügen</button>
    </form>

    <ul>
        {% for task in tasks %}
            <li>{{ task.title }}</li>
        {% else %}
            <li>Keine Aufgaben vorhanden.</li>
        {% endfor %}
    </ul>
</body>
</html>
